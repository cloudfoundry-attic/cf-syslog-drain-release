---
name: scheduler
templates:
  ctl.erb: bin/ctl

packages:
  - scheduler

properties:
  scalablesyslog.scheduler.health:
    description: "The hostport for the scheduler"
    default: ":8080"
  scalablesyslog.scheduler.pprof:
    description: "The hostport for pprof on the scheduler"
    default: "localhost:6060"
